# Copy to .env and fill in real values (never commit .env)
GOOGLE_API_KEY=          # AI Studio key — used by ChatGoogleGenerativeAI + embedding
GOOGLE_CLOUD_PROJECT=    # GCP project ID — used by Vertex AI embedding (rag.py)
TAVILY_API_KEY=          # Tavily search — used by search_company tool
